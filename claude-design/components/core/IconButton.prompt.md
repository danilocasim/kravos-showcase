Icon-only button for row actions, dialog close, and toolbar controls.

```jsx
<IconButton icon="pencil" label="Edit pet" />
<IconButton icon="trash-2" label="Delete pet" variant="danger" />
<IconButton icon="x" label="Close" size="lg" />
```

Use `size="lg"` (44px) for anything a customer taps on mobile.
