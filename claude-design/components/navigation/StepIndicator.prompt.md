Booking progress. Steps are always the same five and always in this order.

```jsx
<StepIndicator steps={['Pet','Services','Groomer','Date & time','Review']} current={2} onStepClick={goBackTo} />
```
