Tab bar. Use only for filtered views of one list; use SideNav for moving between areas of the product.

```jsx
<Tabs value={tab} onChange={setTab} tabs={[{value:'upcoming',label:'Upcoming',count:2},{value:'past',label:'Past'}]} />
```
