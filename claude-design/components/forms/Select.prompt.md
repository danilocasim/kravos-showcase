Closed-list picker. For service or groomer choice in the booking flow use ChoiceCard instead — selection there is a primary decision, not a form detail.

```jsx
<Select placeholder="Select a size" options={[{value:'SMALL',label:'Small'},{value:'MEDIUM',label:'Medium'},{value:'LARGE',label:'Large'}]} />
```
