Single-choice control. Use inside a Field-labelled group; for the booking flow's pet/service/groomer choices use ChoiceCard.

```jsx
<Radio name="size" value="SMALL" checked={size==='SMALL'} onChange={pick} label="Small" description="Under 20 lb" />
```
